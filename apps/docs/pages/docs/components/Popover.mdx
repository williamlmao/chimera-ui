import React from "react";
import { Popover, Input, Showcase } from "chimera-tw";
import {
  InfoCircledIcon,
  Cross2Icon,
  MixerHorizontalIcon,
} from "@radix-ui/react-icons";

## Basic Example

<Showcase>
  <Showcase.Preview>
    <Popover buttonContent={<MixerHorizontalIcon />}>
      <div style={{ display: "flex", flexDirection: "column", gap: 10 }}>
        <p className="font-bold mb-2">Dimensions</p>
        <fieldset className="Fieldset flex items-center justify-between gap-6">
          <label className="text-primary" htmlFor="width">
            Width
          </label>
          <Input
            className="w-32 rounded-md border border-line"
            id="width"
            defaultValue="100%"
          />
        </fieldset>
        <fieldset className="Fieldset flex items-center justify-between gap-6">
          <label className="text-primary" htmlFor="maxWidth">
            Max. width
          </label>
          <Input
            className="w-32 rounded-md border border-line"
            id="maxWidth"
            defaultValue="300px"
          />
        </fieldset>
        <fieldset className="Fieldset flex items-center justify-between gap-6">
          <label className="text-primary" htmlFor="height">
            Height
          </label>
          <Input
            className="w-32 rounded-md border border-line"
            id="height"
            defaultValue="25px"
          />
        </fieldset>
        <fieldset className="Fieldset flex items-center justify-between gap-6">
          <label className="text-primary" htmlFor="maxHeight">
            Max. height
          </label>
          <Input
            className="w-32 rounded-md border border-line"
            id="maxHeight"
            defaultValue="none"
          />
        </fieldset>
      </div>
      <Popover.Close className="PopoverClose" aria-label="Close">
        <Cross2Icon />
      </Popover.Close>
      <Popover.Arrow className="PopoverArrow" />
    </Popover>
  </Showcase.Preview>
</Showcase>

## Customize it by using subcomponents

<Showcase>
  <Showcase.Preview>
    <Popover.Root>
      <Popover.Trigger asChild>
        <button className="IconButton" aria-label="Update dimensions">
          <MixerHorizontalIcon />
        </button>
      </Popover.Trigger>
      <Popover.Portal>
        <Popover.Content className="PopoverContent" sideOffset={5}>
          <div style={{ display: "flex", flexDirection: "column", gap: 10 }}>
            <p className="Text" style={{ marginBottom: 10 }}>
              Dimensions
            </p>
            <fieldset className="Fieldset flex items-center justify-between gap-6">
              <label className="text-primary" htmlFor="width">
                Width
              </label>
              <Input
                className="w-32 rounded-md border border-line"
                id="width"
                defaultValue="100%"
              />
            </fieldset>
            <fieldset className="Fieldset flex items-center justify-between gap-6">
              <label className="text-primary" htmlFor="maxWidth">
                Max. width
              </label>
              <Input
                className="w-32 rounded-md border border-line"
                id="maxWidth"
                defaultValue="300px"
              />
            </fieldset>
            <fieldset className="Fieldset flex items-center justify-between gap-6">
              <label className="text-primary" htmlFor="height">
                Height
              </label>
              <Input
                className="w-32 rounded-md border border-line"
                id="height"
                defaultValue="25px"
              />
            </fieldset>
            <fieldset className="Fieldset flex items-center justify-between gap-6">
              <label className="text-primary" htmlFor="maxHeight">
                Max. height
              </label>
              <Input
                className="w-32 rounded-md border border-line"
                id="maxHeight"
                defaultValue="none"
              />
            </fieldset>
          </div>
          <Popover.Close className="PopoverClose" aria-label="Close">
            <Cross2Icon />
          </Popover.Close>
          <Popover.Arrow className="PopoverArrow" />
        </Popover.Content>
      </Popover.Portal>
    </Popover.Root>
  </Showcase.Preview>
</Showcase>
